[pytest]
# Smoke tests hit real, sometimes billable APIs. They are opt-in only:
#   pytest -m smoke
# Without this, a bare `pytest` (including the pre-push hook) runs them.
addopts = -m "not smoke"
markers =
    smoke: end-to-end tests that call real external APIs (require credentials)
